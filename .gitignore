# Dependency directories
node_modules/
jspm_packages/
web_modules/

# Next.js build output
.next/
out/

# TypeScript build output
dist/
build/

# Local env files (DO NOT COMMIT SENSITIVE KEYS)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local

# SQLite local databases
*.db
*.db-journal
*.db-shm
*.db-wal

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
api-debug.log

# Operating System Files
.DS_Store
Thumbs.db

# IDEs and editors
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Capacitor native builds (we don't ignore these if we want to sync native configurations, but we can ignore build artifacts)
android/app/build/
ios/App/build/
